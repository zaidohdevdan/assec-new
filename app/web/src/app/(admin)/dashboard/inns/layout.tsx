import * as React from "react";

export const metadata = {
  title: "Gerenciar Pousadas",
};

export default function DashboardInnsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
