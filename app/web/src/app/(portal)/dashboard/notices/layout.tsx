import * as React from "react";

export const metadata = {
  title: "Gerenciar Notícias e Avisos",
};

export default function DashboardNoticesLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
