import * as React from "react";

export const metadata = {
  title: "Gerenciar Vídeos (Shorts) - ASSEC",
};

export default function DashboardVideosLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
