import * as React from "react";

export const metadata = {
  title: "Gerenciar Benefícios",
};

export default function DashboardBenefitsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
